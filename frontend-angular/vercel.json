{
  "buildCommand": "npm run build -- --configuration production --output-mode static",
  "outputDirectory": "dist/ProyectoSoftwarePI/browser",
  "installCommand": "npm install --legacy-peer-deps",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
